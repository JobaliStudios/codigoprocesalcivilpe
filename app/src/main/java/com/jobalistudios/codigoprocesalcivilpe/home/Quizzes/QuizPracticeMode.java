package com.jobalistudios.codigoprocesalcivilpe.home.Quizzes;

/** Defines the local pool used to build a quiz session. */
public enum QuizPracticeMode {
    ALL,
    FAVORITES,
    PREVIOUS_ERRORS,
    SECTION,
    QUICK_REVIEW
}
